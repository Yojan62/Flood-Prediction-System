import React from 'react';

// Defines the Footer component for the application.
function Footer() {
  // Returns the JSX structure for the footer.
  return (
    // Uses the standard HTML footer element.
    <footer>
      {/* Contains a paragraph with the copyright information. */}
      <p>© 2025 Flood Prediction Project. All rights reserved.</p>
    </footer>
  );
}

// Exports the Footer component so it can be used in other files (like App.js).
export default Footer;